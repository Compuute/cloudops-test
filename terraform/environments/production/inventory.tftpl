---
# generated by terraform — do not edit manually
all:
  children:
    webservers:
      hosts:
%{ for i, ip in servers ~}
        ${names[i]}:
          ansible_host: ${ip}
          ansible_user: deploy
          env: ${env}
%{ endfor ~}
