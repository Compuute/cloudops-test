# ansible-vault encrypted — edit with: ansible-vault edit group_vars/all/vault.yml
# unencrypted structure lives in vault.example.yml (gitignored)
$ANSIBLE_VAULT;1.1;AES256
# placeholder — replace this line with actual encrypted content before running
