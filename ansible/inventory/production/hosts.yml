---
all:
  children:
    webservers:
      hosts:
        prod-app-01:
          ansible_host: 10.0.2.10
          ansible_user: deploy
          ansible_ssh_private_key_file: "{{ vault_ssh_private_key_path }}"
          env: production
        prod-app-02:
          ansible_host: 10.0.2.11
          ansible_user: deploy
          ansible_ssh_private_key_file: "{{ vault_ssh_private_key_path }}"
          env: production
