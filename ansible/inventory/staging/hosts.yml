---
all:
  children:
    webservers:
      hosts:
        staging-app-01:
          ansible_host: 10.0.1.10
          ansible_user: deploy
          ansible_ssh_private_key_file: "{{ vault_ssh_private_key_path }}"
          env: staging
